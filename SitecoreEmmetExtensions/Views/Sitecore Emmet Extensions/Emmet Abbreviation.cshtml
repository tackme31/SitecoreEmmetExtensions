@using SitecoreEmmetExtensions.Extensions
@{
    var param = RenderingContext.Current.Rendering.Parameters["Abbreviation"];
    var abbreviation = string.IsNullOrWhiteSpace(param) ? "div" : param;
}
@Html.Sitecore().RenderEmmetAbbreviation(abbreviation)